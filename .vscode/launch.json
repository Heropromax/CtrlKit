{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug CMake",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal",
            "preLaunchTask": "CMake: build",
            "stopAtEntry": false,
        }
    ]
}